///
/// Value is "logo" || "full"
const String splashScreenType = 'full';

///
/// String hex color. Default white
const String splashScreenColor = '#ffffff';

///
/// Type is "fill" || "fit-width" || "fit-height" || "scale-down" || "contain" || "none" || "cover"
const String splashScreenBoxFit = 'fill';
const double splashScreenLogoWidth = 500;
const double splashScreenLogoHeight = 500;
